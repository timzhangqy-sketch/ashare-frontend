---
description: A股量化交易终端 UI 设计规范 v2.0 — Cursor Agent 在修改任何前端 CSS、样式、布局时必须遵循的规则。
globs:
  - "src/**/*.tsx"
  - "src/**/*.ts"
  - "src/**/*.css"
  - "src/**/*.scss"
alwaysApply: false
---

# A股量化终端 UI 设计规范 — Cursor 执行规则

本规则是前端样式修改的唯一权威。违反这些规则等于 bug。

---

## 字重铁律（最常犯错的地方）

只用三档字重，绝不用中间值：
- `400`：正文、说明、辅助文字、helper text、placeholder、标签描述
- `500`：表格数据、导航标签
- `700`：标题（H1/H2）、KPI 大数字、Stat 数字、按钮文字

`600` 仅用于：H3/H4 标题、badge/tag 标签、Stat 卡标签。不在其他任何地方使用。

❌ 绝对禁止：
- 说明文字用 500 或 600
- KPI 标签用 600（标签必须 400）
- 辅助文字（helper/caption）用 500

---

## 字号层级（不可偏离）

| 元素 | font-size | font-weight |
|------|-----------|-------------|
| 页面主标题 (H1) | 22px | 700 |
| 模块标题 (H2) | 17px | 650 |
| 卡片标题 (H3) | 14px | 600 |
| 正文 / 说明 | 13px | 400 |
| 表格数据 | 13px | 500 |
| 辅助文字 / 表头 | 12px | 400 |
| 脚注 / 空态 / caption | 11px | 400 |
| badge / tag | 11px | 600 |
| KPI 大数字 | 28px | 700 |
| KPI-sm 数字 | 20px | 700 |
| Stat 数字 | 24px | 700 |

如果你需要的字号不在此表中，不要自行发明——使用最接近的规范值。

---

## 金融数字规则

所有金融数字（价格、涨跌幅、成交量、KPI、Stat）必须：
```css
font-variant-numeric: tabular-nums;
```

KPI / Stat 大数字额外加：
```css
letter-spacing: -0.02em;
```

---

## 零值弱化规则

当 KPI 或 Stat 的值为 0、'—'、null 时：
- 数字颜色必须降为 `var(--text-muted)` (#5A6B85)
- 不使用语义色

---

## KPI 语义色（Dashboard 专用）

| 指标 | 值 > 0 | 值 = 0 |
|------|--------|--------|
| 买点机会 | --text-primary | --text-muted |
| 共振信号 | --info (#3B82F6) | --text-muted |
| 标的精选 | --text-primary | --text-muted |
| 可买计机会 | --info (#3B82F6) | --text-muted |
| 风控拦截 | --warn (#F59E0B) | --text-muted |
| 高风险持仓 | --critical (#DC2626) | --text-muted |
| 持仓数量 | --text-primary | --text-muted |
| 卖出提示 | --up (#EF4444) | --text-muted |
| Pipeline | 正常 --source-real / 异常 --critical | — |

---

## 间距规则

- 页面 padding: `16px 20px`
- Section 之间: `16px`
- 卡片内 padding: `16px`（紧凑 `12px`）
- KPI grid gap: `12px`
- 四象限 grid gap: `12px`
- 所有间距必须是 4px 的倍数

---

## Dashboard 版面结构（v2）

Dashboard 必须按以下顺序从上到下排列：

1. **页头**（40px）：标题 + 按钮，无描述段落
2. **今日变化条**（~56px）：6 指标水平排列
3. **行动清单**（~64-96px）：三行紧凑内联，chip 只含名称+涨幅
4. **KPI 快览条**（~72px）：单行 8 指标 + Pipeline/版本
5. **四象限**（~400px）：2×2 grid，机会/风控/组合/系统健康
6. **底栏**（24px）：时间戳 + 版本

❌ Dashboard 中禁止的元素：
- 页面描述段落
- Section 描述段落
- 漂浮的独立跳转链接
- Pipeline/版本作为独立 KPI 卡
- 行动清单 chip 内的策略标签/Gate 状态
- 任何 min-height 强制撑高

---

## 颜色规则

- 涨/买入/正收益：`--up` (红)
- 跌/卖出/负收益：`--down` (绿)
- 预警/风控拦截：`--warn` (橙)
- 信息/共振：`--info` (蓝)
- 品牌/CTA：`--accent` (青)
- 错误/Gate拦截：`--critical` (深红)
- 共振/多策略：`--purple` (紫)

侧边栏永远深色，不随主题切换。

---

## 卡片规则

| 属性 | 标准卡片 | KPI 卡 |
|------|---------|--------|
| border-radius | 8px | 10px |
| border | 1px solid var(--border-default) | 同左 |
| hover border | var(--border-strong) | 同左 |
| hover bg | var(--bg-hover) | 同左 |
| padding | 16px | 16px |

---

## 动画规则

- hover: 120ms ease-out
- Tab 切换: 200ms
- 页面切换: 无动画（即时切换）
- ❌ 禁止: fade-in、fly-in、数字跳动、>300ms 动画
